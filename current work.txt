2025-11-11 — Documentation hardening pass

- Updated `STANDARDS.md`
  - Enforced Writer-only terminology across schemas, queries, and UI.
  - Added Image & Hero Rendering Policy to prevent unintended cropping/upscaling.
  - Codified cookie key `user-country` and precedence order.

- Updated `OPERATIONAL_RULES.md`
  - Added Cookies & Routing Conventions (use `user-country`, deprecate `/author/*`).

- Added `PROJECT_DOCUMENTATION.md`
  - Architecture map, data flow, Writer-only terminology, image and cookie policies.

- Updated `README.md`
  - Switched Authors→Writers in content sections.
  - Added links to project documentation (standards, ops rules, runbook, project docs).

Next:
- Visual QA on hero/spotlight images per new policy.
- Manual update of 16 articles in Sanity (content, writer attribution, images).
- Minor documentation refinements after QA (image/cookie policies consistency).

Notes:
- Manual 16-article update estimate: ~4–5.5 hours depending on content complexity.
